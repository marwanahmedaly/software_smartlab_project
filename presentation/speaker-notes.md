# SmartLab Presentation — Speaker Notes
## CISC 818 – Software Engineering with AI Milestone 3
**Date:** 2025-06-05 · **Total Time:** 35 minutes · **Team of 4**

---

## How to Use These Notes

- **🎯 Bold text** = key points to emphasize
- **⏱️ Time cues** = how long each section should take
- **💬 Quotes** = suggested exact wording
- **👆 [Action]** = physical action (click, gesture, handoff)
- **📝 Slide #** = reference to the HTML slide deck

---

# Marwan Aly — Speaker Notes
**Total Speaking Time:** ~12 minutes (6.5 min pre-reflection + 5 min reflection)

---

## 📝 Slide 1: Title Slide (20 sec)

*No speaking needed — let audience read. Stand confidently, smile.*

**⏱️ 20 seconds**

---

## 📝 Slide 2: The Problem (60 sec)

**Opening:**
> "Good [morning/afternoon]. I'm Marwan, and this is our team: Mohamed, Manar, and Aya. We're here to present **SmartLab** — an AI-powered computer laboratory management system we built over six weeks."

**Problem Statement:**
> "Computer labs are critical infrastructure in universities, but their management is still largely **reactive and manual**. Equipment failures are discovered only when users complain. Repair histories live in scattered spreadsheets. Technicians have no structured queue, and administrators have zero visibility into hardware health."

**Impact:**
> "The result? **Preventable downtime** that disrupts teaching — especially during exam periods when a single projector failure can force rescheduling an entire lab session."

**Users:**
> "We identified three user groups: **administrators** who need budget planning data, **technicians** who need prioritised repair queues and diagnostic guidance, and **regular users** — students and staff — who need a simple way to report faults."

**Purpose:**
> "Our goal was to reduce downtime and lower IT overhead, aligning with **UN SDG 4: Quality Education**."

**👆 [Action]:** Click to next slide.

**⏱️ 60 seconds total**

---

## 📝 Slide 3: System Architecture (60 sec)

**Opening:**
> "Before we show the system, let me explain how it fits together."

**Architecture:**
> "SmartLab is a **four-tier web application**. The frontend is vanilla HTML, CSS, and JavaScript — zero framework overhead, runs on any browser. It communicates via HTTP REST to our **Node.js/Express backend**, which handles JWT authentication and business logic."

**Data Layer:**
> "For persistence, we chose **SQLite** — zero-config, file-based, perfect for a single institution. For AI diagnostics, we integrated **Llama 3.2 via OpenRouter** — an on-premise alternative to cloud LLMs that keeps data within the institution."

**Data Flow:**
> "Here's the flow: a user clicks 'Report Fault' — the frontend sends a POST request to `/api/issues`. The backend validates the JWT, saves to SQLite, and notifies technicians. When a technician opens AI Diagnostics, the backend enriches the prompt with device specs and fault history, calls Llama 3.2, and returns an Arabic diagnosis in under 10 seconds."

**👆 [Action]:** Click to next slide.

**⏱️ 60 seconds total**

---

## 📝 Slide 4: Live Demo (3 min)

**Setup:**
> "Let me show you this in action with a realistic use case."

**Step 1 — User Reports Fault:**
> "First, I log in as a regular user. This is **user@lab.com**. I navigate to Device PC-05 and report a fault: the screen is flickering, hardware category. I submit the form. **This fulfills US-07 from our requirements document: User Submits a Fault Report.**"

**👆 [Action]:** Switch browser tab to SmartLab, login as user, navigate to device, submit fault. Or play screen recording.

**Step 2 — Technician Views Queue:**
> "Now I switch to the technician view. I log in as **tech@lab.com** and open the Repair Queue. The new fault appears at the top, prioritised by severity. **This fulfills US-08: Technician Views Repair Queue.**"

**👆 [Action]:** Login as tech, open repair queue, click the new fault.

**Step 3 — AI Diagnostics:**
> "I click 'AI Diagnostics.' The chat panel opens, pre-loaded with the device specs and fault description. After a moment, **Llama 3.2 responds with step-by-step Arabic suggestions**: check the HDMI cable, test with another monitor, inspect the graphics card. **This fulfills US-09: Technician Uses AI Chatbot for Diagnostics.**"

**👆 [Action]:** Click AI Diagnostics, show response.

**Step 4 — Update Status:**
> "Following the AI suggestion, I replace the HDMI cable and update the status: Open → In Progress → Resolved. I add a resolution note: 'Replaced HDMI cable.' **This fulfills US-10: Technician Updates Report Status.**"

**👆 [Action]:** Update status, enter resolution note.

**Step 5 — Admin Views Reports:**
> "Finally, I log in as **admin@lab.com** and open the Reports module. I set a date range, and the system renders a bar chart showing faults by month, plus KPI cards: average repair time, most-faulted device, total maintenance count. I can also see Smart Alerts generated by our daily cron job. **This fulfills US-11: Admin Views Fault Analytics Dashboard.**"

**👆 [Action]:** Login as admin, show reports and alerts.

**Closing:**
> "That was our system in action. Now Aya will walk you through how AI shaped every stage of our development process."

**👆 [Action]:** Hand over to Aya with a gesture.

**⏱️ 3 minutes total**

---

## 📝 Slide 8: AI Usage — Backend Implementation (90 sec)

*This is during Segment 2 (AI Usage Across SDLC), after Mohamed finishes Frontend.*

**Opening:**
> "For the backend, I used Claude Code and GitHub Copilot to scaffold the Express server, JWT authentication, and SQLite schema."

**Prompt:**
> "My prompt was: *'Create Express server with JWT middleware, SQLite setup, and REST API routes for devices and issues with role-based access control.'*"

**Outcome:**
> "The AI generated route skeletons, auth middleware patterns, and a 5-table SQLite schema."

**Revision — Critical:**
> "But I had to revise two critical things. First, the AI generated JWT middleware with a **hardcoded secret** — `JWT_SECRET = 'secret'`. I revised this to use `process.env.JWT_SECRET` for security. Second, the AI schema was missing **foreign key constraints** — I added them manually to enforce referential integrity."

**Team Decision:**
> "Our team also evaluated MongoDB versus SQLite. We chose SQLite because it's zero-config, file-based, and perfect for a single-institution deployment with no external dependencies."

**Limitation:**
> "The limitation? AI initially suggested MongoDB with Mongoose — we had to override that. Some SQL syntax was also generic and needed refinement for the better-sqlite3 dialect."

**👆 [Action]:** Hand off to Mohamed.

**⏱️ 90 seconds**

---

## 📝 Slide 12: AI Usage — Deployment (60 sec)

*This is the last slide of Segment 2.*

**Opening:**
> "For deployment, I asked Claude to create a Docker Compose setup."

**Prompt:**
> "Prompt: *'Create a Docker Compose setup for Node.js + SQLite application.'*"

**Outcome:**
> "It generated a `docker-compose.yml` with volume mounts."

**Revision:**
> "But the AI didn't configure the SQLite volume persistence path correctly — I fixed the path mapping manually to `./data:/app/data`."

**Team Decision:**
> "We made Docker optional for local development and used direct `npm start` for the demo to minimize complexity."

**Limitation:**
> "AI suggested a multi-stage build with Alpine Linux — we simplified to a single-stage build for faster build times."

**Closing Segment 2:**
> "That's how AI supported every SDLC stage. Now each of us will reflect on our personal experience. I'll go first."

**👆 [Action]:** Hand off to yourself — transition to reflection.

**⏱️ 60 seconds**

---

## 📝 Slide 13–14: Marwan's Individual Reflection (5 min)

**Opening:**
> "My specific task was building the JWT authentication and REST API using AI."

**Tool & Purpose:**
> "I used **Claude Code and GitHub Copilot**. I chose AI because it accelerates boilerplate scaffolding, allowing me to focus on security logic and business rules instead of writing route handlers from scratch."

**Input & Output:**
> "My prompt was: *'Create Express server with JWT middleware, SQLite setup, and REST routes with role-based access.'* The AI produced route skeletons, an auth middleware pattern, and SQLite table definitions."

**How I Revised It — KEY POINT:**
> "Here's what I changed: the AI generated middleware with `JWT_SECRET = 'secret'` hardcoded. **I revised this to use environment variables** — `process.env.JWT_SECRET`. The AI schema also missed foreign key constraints. **I added them manually** to enforce referential integrity between devices, issues, and users."

**What Was Useful:**
> "The AI was incredibly useful for rapid prototyping — I had a working auth system in hours instead of days."

**What Was Challenging:**
> "The challenge was that security-critical code needs manual review. AI doesn't understand timing attacks, secret rotation, or the principle of least privilege."

**Limitations:**
> "AI suggested MongoDB initially, which would have added unnecessary complexity. Some SQL syntax was also too generic."

**Future:**
> "Going forward, I'll use AI for scaffolding but **personally review all authentication and security code** before committing. AI is an assistant, not a security auditor."

**👆 [Action]:** Hand off to Mohamed.

**⏱️ 5 minutes**

---

# Mohamed Othman — Speaker Notes
**Total Speaking Time:** ~7.5 minutes (2.5 min pre-reflection + 5 min reflection)

---

## 📝 Slide 7: AI Usage — System Design (90 sec)

*This is during Segment 2, after Aya finishes Planning.*

**Opening:**
> "For system design, I used Claude Code to generate the architecture description and Google Stitch with Figma for wireframes."

**Prompt:**
> "My prompt to Claude was: *'Describe a three-tier architecture for a lab management system with Node.js backend and vanilla JS frontend.'*"

**Outcome:**
> "Claude produced a correct layered architecture. For UI, Stitch generated initial mockups from text descriptions, which I imported into Figma for refinement."

**Team Decision — KEY POINT:**
> "Our team evaluated **microservices versus monolithic architecture**. We chose monolithic Express because: single server deployment, easier SQLite management, and no DevOps overhead for a 6-week timeline. Microservices would have been over-engineering for our scope."

**Limitation:**
> "The limitation? Stitch produced consumer-style gradients and rounded cards — I manually simplified everything for institutional information density."

**👆 [Action]:** Hand off to Marwan.

**⏱️ 90 seconds**

---

## 📝 Slide 9: AI Usage — Frontend Implementation (60 sec)

*This is during Segment 2, after Marwan finishes Backend.*

**Opening:**
> "For the frontend, I used Claude Code to generate the RTL Arabic CSS and component structure."

**Prompt:**
> "Prompt: *'Create RTL Arabic CSS layout with right sidebar, status badges, and card-based dashboard for a lab management system.'*"

**Outcome:**
> "The AI produced a base RTL flexbox framework that I could build on."

**Revision — KEY POINT:**
> "But I had to make two key revisions. First, AI used `margin-right: 260px` for sidebar spacing — **I revised this to `margin-inline-end: 260px`** for true logical properties that work across languages. Second, AI generated SVG circles for the lab map nodes — **I replaced them with rectangles** to match actual PC form factors."

**Limitation:**
> "AI struggled with RTL nuances — it uses physical directions like 'left' and 'right' instead of logical properties. Consumer-style designs also needed manual simplification for a professional lab management context."

**👆 [Action]:** Hand off to Manar.

**⏱️ 60 seconds**

---

## 📝 Slide 15–16: Mohamed's Individual Reflection (5 min)

**Opening:**
> "My specific task was creating the RTL responsive UI and the interactive SVG lab map."

**Tool & Purpose:**
> "I used **Claude Code and Google Stitch/Figma**. I chose AI because RTL CSS is tedious and error-prone to write from scratch, and Stitch accelerated the wireframing process significantly."

**Input & Output:**
> "My prompt was: *'Create RTL Arabic CSS layout with right sidebar, status badges, and card dashboard.'* The AI produced an RTL flexbox layout, sidebar structure, and SVG map node scaffolding."

**How I Revised It — KEY POINT:**
> "I made two critical revisions. First, AI used `margin-right: 260px` — **I replaced this with `margin-inline-end: 260px`** for true logical properties that support true internationalization. Second, AI generated SVG circles for map nodes — **I replaced them with rectangles** to match actual PC form factors."

**What Was Useful:**
> "I got a working base immediately without writing every flexbox rule from scratch."

**What Was Challenging:**
> "The challenge was that AI doesn't truly understand RTL — it thinks in physical directions, not logical properties. I also had to manually simplify consumer-style gradients and rounded cards for an institutional look."

**Limitations:**
> "AI-produced designs were too 'consumer app' — heavy shadows, rounded corners — I had to flatten everything for professional lab management density."

**Future:**
> "Going forward, I'll use AI for layout scaffolding but **budget 30% of UI time for human design polish**. AI doesn't understand institutional aesthetic — it thinks every app should look like Instagram."

**👆 [Action]:** Hand off to Manar.

**⏱️ 5 minutes**

---

# Manar Elabsi — Speaker Notes
**Total Speaking Time:** ~6.5 minutes (1.5 min pre-reflection + 5 min reflection)

---

## 📝 Slide 10: AI Usage — AI Integration (90 sec)

*This is during Segment 2, after Mohamed finishes Frontend.*

**Opening:**
> "For AI integration, I used Claude Code to connect our system to Llama 3.2 via OpenRouter."

**Prompt:**
> "My prompt was: *'Create Node.js service calling OpenRouter API for fault diagnosis with device context.'*"

**Outcome:**
> "The AI generated an API wrapper — it's in a file called `deepseek.js`, which is a legacy name from early prototyping. The actual model is Llama 3.2. It also scaffolded the `/api/ai/diagnose` route and the predictive alerts cron job."

**Revision — KEY POINT:**
> "I made two major revisions. First, AI wrote a generic `fetch()` call with a static prompt. **I revised it to dynamically inject device specs and issue history from SQLite** into the prompt, making the diagnosis contextual and useful. Second, AI set the cron job to run every minute — `* * * * *`. **I revised this to daily at midnight** to avoid hitting API rate limits."

**Limitation — KEY POINT:**
> "The key limitation was that AI could write the **code to call the LLM**, but it couldn't design effective **prompts**. Response quality depended entirely on human context engineering — enriching the prompt with device type, OS, and fault history."

**👆 [Action]:** Hand off to Aya.

**⏱️ 90 seconds**

---

## 📝 Slide 17–18: Manar's Individual Reflection (5 min)

**Opening:**
> "My specific task was integrating Llama 3.2 via OpenRouter for AI diagnostics and building the predictive alerts cron job."

**Tool & Purpose:**
> "I used **Claude Code**. I chose AI because API integration and cron job scheduling involve many async and error-handling moving parts that are easy to get wrong."

**Input & Output:**
> "My prompt was: *'Create Node.js service calling OpenRouter API for fault diagnosis with device context.'* The AI produced the `deepseek.js` wrapper — legacy filename — the `/api/ai/diagnose` route, and the `predictive.js` cron job."

**How I Revised It — KEY POINT:**
> "Two major revisions. First, AI wrote a generic fetch with a static prompt. **I revised it to dynamically inject device specs and issue history from SQLite**, making the diagnosis actually useful. Second, AI set the cron to run every minute. **I changed it to daily at midnight** to respect API rate limits."

**What Was Useful:**
> "The AI accelerated my understanding of fetch error boundaries and async patterns."

**What Was Challenging:**
> "The challenge was that **prompt engineering was entirely manual**. AI could write the code to call the LLM, but it couldn't design effective prompts. Response quality depended entirely on human context engineering."

**Limitations:**
> "The file is still named `deepseek.js` from early prototyping — our actual model is Llama 3.2. Also, AI couldn't predict which prompt structures would yield useful diagnostic suggestions."

**Future:**
> "Going forward, I'll **invest more time in prompt design upfront**. AI helps with integration code, but LLM behavior requires human experimentation. I'd prototype prompts before writing any integration code."

**👆 [Action]:** Hand off to Aya.

**⏱️ 5 minutes**

---

# Aya Abdelmonem — Speaker Notes
**Total Speaking Time:** ~9.5 minutes (4 min pre-reflection + 5 min reflection + 0.5 min closing)

---

## 📝 Slide 5: AI Methodology Intro (30 sec)

*This is the opening of Segment 2.*

**Opening:**
> "Before we dive into specifics, let me share our team's AI methodology."

**Core Principle:**
> "Our principle was **'AI as reviewer > generator.'** We used AI to identify gaps and scaffold structure, but we critically evaluated every output."

**Stats:**
> "Our stats: 40% of AI output was accepted with minor edits, 45% was substantially rewritten, and 15% was discarded entirely."

**Logging:**
> "Every team member maintained a personal AI Usage Log documenting the tool, SDLC phase, prompt, output, and reflection. This is the evidence base for what you're about to hear."

**👆 [Action]:** Hand off to Mohamed.

**⏱️ 30 seconds**

---

## 📝 Slide 6: AI Usage — Planning & Requirements (90 sec)

*This is the first content slide of Segment 2.*

**Opening:**
> "For planning and requirements, I used Claude Code to review our Assignment 1 proposal before we started coding."

**Prompt:**
> "Prompt: *'Review our lab management proposal and identify scope gaps: AI model specifics, offline behavior, feature prioritization.'*"

**Outcome:**
> "Claude flagged three critical gaps: we hadn't specified which Llama model to use, we were missing offline behavior handling, and the interactive lab map was misclassified as a Core feature when it should have been Non-Core."

**Team Decision — KEY POINT:**
> "Based on this analysis, our team made a deliberate decision: **we downgraded the interactive map from Core to Non-Core** to protect our MVP timeline under the 6-week constraint. This ensured the core fault-reporting and diagnostic workflows remained functional."

**Limitation:**
> "The limitation? Claude also proposed push notifications and a PWA — we discarded these as out of scope."

**👆 [Action]:** Hand off to Mohamed.

**⏱️ 90 seconds**

---

## 📝 Slide 11: AI Usage — Testing & QA (90 sec)

*This is during Segment 2, after Manar finishes AI Integration.*

**Opening:**
> "For testing and QA, I used Claude Code and Copilot to generate test cases and SQL queries."

**Prompt:**
> "Prompt: *'Write test cases for role-based access control and SQLite queries for maintenance summary reports with date filtering.'*"

**Outcome:**
> "The AI generated test scaffolding and report queries."

**Revision — KEY POINT:**
> "But I found a subtle bug. The AI query used `BETWEEN '2024-01-01' AND '2024-01-31'` for date filtering. **I revised this to `>= '2024-01-01' AND < '2024-02-01'`** because SQLite's `BETWEEN` is inclusive and misses time-of-day boundaries — a classic gotcha."

**Limitation:**
> "AI tests covered happy paths only. I had to add edge cases manually: expired JWT tokens, duplicate email handling, and SQL injection attempts."

**👆 [Action]:** Hand off to Marwan for Deployment.

**⏱️ 90 seconds**

---

## 📝 Slide 19–20: Aya's Individual Reflection (5 min)

**Opening:**
> "My specific task was building the reports dashboard with Chart.js, writing analytics SQL queries, and maintaining project documentation."

**Tool & Purpose:**
> "I used **Claude Code and GitHub Copilot**. I chose AI because documentation and SQL aggregations are structured but tedious — tasks where AI excels at boilerplate generation."

**Input & Output:**
> "My prompt was: *'Write SQLite queries for maintenance summary with date filtering, including average repair time and most-faulted device.'* The AI produced Chart.js configuration, SQL aggregation queries, and README.md and PLAN.md drafts."

**How I Revised It — KEY POINT:**
> "Two revisions. First, the AI query used `BETWEEN` for date filtering, which misses boundaries in SQLite. **I revised it to use `>=` and `<` operators**. Second, the AI README had placeholder `.env` values like `your_secret_here`. **I replaced these with actual `.env.example` content** so new developers could get started immediately."

**What Was Useful:**
> "Documentation scaffolding saved hours of formatting and structure planning."

**What Was Challenging:**
> "The challenge was that AI-generated SQL had subtle date boundary bugs — queries returned wrong aggregates until I tested them against real seed data."

**Limitations:**
> "AI suggested complex nested JOINs that performed poorly on SQLite — I simplified them to targeted queries. The documentation also included features we hadn't built yet, which I had to remove."

**Future:**
> "Going forward, I'll use AI for **structure and drafts**, but always **run queries against real data and benchmark performance** before accepting. For documentation: generate the skeleton, then fill in only implemented features."

**👆 [Action]:** Transition to closing.

**⏱️ 5 minutes**

---

## 📝 Slide 21: Closing (30 sec)

**Opening:**
> "To close, our key takeaway is this: AI accelerated our 6-week development cycle significantly, but **human judgment remained essential at every stage**."

**Evidence:**
> "Every AI interaction was logged in our personal AI Usage Logs. The system you saw today is runnable, demonstrable, and requirement-complete per Assignment 2."

**Closing:**
> "Thank you — we're happy to take questions."

**👆 [Action]:** Open palms to audience, invite questions.

**⏱️ 30 seconds**

---

# Handoff Script Summary

| # | From | To | Script |
|---|------|-----|--------|
| 1 | Marwan (Demo) | Aya (AI Usage) | "That was our system in action. Now Aya will walk you through how AI shaped every stage of our development process." |
| 2 | Aya (Planning) | Mohamed (Design) | "That's how AI helped us plan. Mohamed will now explain how it shaped our system design." |
| 3 | Mohamed (Design) | Marwan (Backend) | "Our design was ready — Marwan, take us into implementation." |
| 4 | Marwan (Backend) | Mohamed (Frontend) | "Backend was solid. Mohamed, how did AI help on the frontend?" |
| 5 | Mohamed (Frontend) | Manar (AI) | "The interface was built. Manar, how did we integrate the AI brain?" |
| 6 | Manar (AI) | Aya (Testing) | "AI was wired in. Aya, how did we test and validate everything?" |
| 7 | Aya (Testing) | Marwan (Deployment) | "We tested thoroughly. Marwan, how did we deploy?" |
| 8 | Marwan (Deployment) | All (Reflections) | "That's how AI supported every SDLC stage. Now each of us will reflect on our personal experience. Marwan first." |
| 9 | Marwan (Reflection) | Mohamed (Reflection) | "Mohamed, your turn." |
| 10 | Mohamed (Reflection) | Manar (Reflection) | "Manar?" |
| 11 | Manar (Reflection) | Aya (Reflection) | "Aya, close us out." |
| 12 | Aya (Reflection) | Audience (Q&A) | "Thank you — we're happy to take questions." |

---

# Timing Cheat Sheet

| Segment | Speaker | Time | Cumulative |
|---------|---------|------|------------|
| Title | Marwan | 0:20 | 0:20 |
| Problem | Marwan | 1:00 | 1:20 |
| Architecture | Marwan | 1:00 | 2:20 |
| Demo | Marwan | 3:00 | 5:20 |
| AI Methodology | Aya | 0:30 | 5:50 |
| AI Planning | Aya | 1:30 | 7:20 |
| AI Design | Mohamed | 1:30 | 8:50 |
| AI Backend | Marwan | 1:30 | 10:20 |
| AI Frontend | Mohamed | 1:00 | 11:20 |
| AI Integration | Manar | 1:30 | 12:50 |
| AI Testing | Aya | 1:30 | 14:20 |
| AI Deployment | Marwan | 1:00 | 15:20 |
| Marwan Reflection | Marwan | 5:00 | 20:20 |
| Mohamed Reflection | Mohamed | 5:00 | 25:20 |
| Manar Reflection | Manar | 5:00 | 30:20 |
| Aya Reflection | Aya | 5:00 | 35:20 |
| Closing | Aya | 0:30 | 35:50 |

> **Note:** Target is 35:00. The cheat sheet shows 35:50, so speakers should aim for the lower end of their ranges. Aya should keep methodology to 20 seconds and closing to 20 seconds to save 20 seconds. Marwan can trim 10 seconds from demo. This brings total to ~35:00.

---

*End of Speaker Notes*
